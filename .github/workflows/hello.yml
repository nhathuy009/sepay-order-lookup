name: Secret Demo

on:
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest

    env:
      MY_NAME: ${{ secrets.MY_NAME }}

    steps:
      - name: Đọc Secret
        run: |
          echo "Xin chào $MY_NAME"
